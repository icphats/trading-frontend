/**
 * ICRC Ledger Type Barrel
 *
 * Type-only re-exports from Candid declarations.
 * All runtime logic lives in repositories (token.repository.ts).
 */

export type { _SERVICE as LedgerService, Account } from 'declarations/icrc_ledger/icrc_ledger.did';
